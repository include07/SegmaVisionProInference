# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
build/
dist/

# Docker
db_data/

# Environment variables
.env

# IDE/Editor specific
.vscode/
.idea/
*.swp
*.swo
# ... (existing ignores) ...

# OR if .gitignore is in backend/ just use uploads/
# uploads/

inference/pre_trained_models/*.pth

keep the uploads directory but ignore the folders in it
# Ignore all folders in uploads but keep the directory itself
backend/uploads/*/
# Ignore all folders in uploads but keep the directory itself

# ignore all files in inference/hf_cache
inference/hf_cache/hub